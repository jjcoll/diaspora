# API base URL. Leave empty to use Vite's dev proxy (which forwards
# /api/* to localhost:8000). Set in production / preview to the deployed
# backend URL, e.g. VITE_API_BASE=https://diaspora-backend.fly.dev
VITE_API_BASE=
